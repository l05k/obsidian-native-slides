---
deck: []
author: Native Slides
tags:
  - demo
---

# Properties demo

This deck (card) note has frontmatter properties (`author`, `tags`) besides the reserved `deck` key.

In the default edit view the in-note properties panel shows normally (native behavior). Enter **Slides mode** (command `Mod+Shift+E`): the editor becomes immersive — tab bar and sidebars hide, the bottom bar takes the tab bar's height, and the in-note properties panel hides while editing.
